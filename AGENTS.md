# リポジトリガイドライン

## プロジェクト概要

- このリポジトリは Web アプリ経由で操作できる自動給餌器の Raspberry Pi サーバである．
- 利用者は事前に登録された少人数のみとし，Tailscale を用いてアクセスする．
- 以下の機能を提供する．
  - Web アプリ経由での手動遠隔給餌
  - SQLite に保存した日時による 1 回限りのスケジュール給餌
  - 給餌スケジュールの一覧表示，追加，削除
  - Web カメラによる監視
  - 現在のサーバ時刻および最終給餌時刻の表示
  - 最新 10 件の給餌履歴の表示
  - 給餌クールタイムとサーボ駆動時間の設定

## アーキテクチャ

- Raspberry Pi 上で Axum サーバを動作させる．
- Web UI は Leptos を利用する．
- 初回表示はサーバサイドレンダリングし，Leptos のハイドレーション後は給餌とスケジュール操作を非同期で行う．
- サーバ側の状態変化は Server-Sent Events（SSE）で開いているトップ画面へ通知する．
- GPIO 制御は Rust から行う．
- スケジュール給餌は Raspberry Pi 単体で動作し，外部サービスに依存しない．
- スケジュールと最終給餌時刻は SQLite に保存し，`sqlx` から操作する．
- スケジュールはサーバのローカル日時で管理する．未来の日時だけを登録可能とする．
- スケジュールは指定日時に給餌が成功すると消費され，SQLite から削除される．
- 手動給餌とスケジュール給餌は共通の給餌サービスを使用し，排他制御とクールタイム判定を行う．
- 設定は SQLite に保存する．クールタイムの既定値は 300 秒，一回のサーボ駆動時間の既定値は 1,000 ミリ秒とする．
- 指定日時を過ぎた未実行スケジュールは給餌せず，給餌失敗として一覧に残す．
- 旧形式の時刻のみのスケジュールは日時未設定として保持し，自動実行しない．

### モジュール構成

- `src/main.rs`: 各機能の初期化，ルータの結合，サーバの起動を行う．
- `src/api.rs`: HTML 生成と HTTP ハンドラを提供し，フォームと非同期 API の共通処理を管理する．
- `src/feed.rs`: サーボモータ，モック動作，排他制御，クールタイムを含む共通給餌処理を管理する．
- `src/camera.rs`: カメラ映像の配信，ルート定義，モック動作を管理する．
- `src/schedule.rs`: SQLite 操作，時刻の検証，スケジューラを管理する．
- `src/lib.rs`: Leptos UI，ハイドレーション，ブラウザとサーバで共有するデータ型を提供する．

## 主要技術

- Raspberry Pi 4
- Rust
- Axum
- Leptos
- Tokio
- SQLite / SQLx
- Tailscale

## 設計方針

- 必要十分な機能をシンプルに実装し，保守性を最優先とする．
- 小規模な個人利用を前提とする．
- Raspberry Pi 単体で完結する設計を優先する．
- 外部サービスへの依存は必要最小限とする．
- シンプルさを優先し，過度な抽象化や将来の拡張を見越した設計は避ける（YAGNI を意識する）．
- 既存の設計との一貫性を保つことを優先する．

## コーディング規約

- Rust API Guidelines および一般的な Rust の命名規則に従う．
- `cargo fmt` および `leptosfmt` による整形を前提とする．
- `panic!` は通常のエラー処理に利用しない．
- `unsafe` は必要最小限に留める．
- アプリケーション層では `color_eyre` を利用する．
- ライブラリ層では `thiserror` を利用する．

## Web UI

- UI は Leptos を用いて実装する．
- JavaScript を直接記述するよりも，Leptos の機能を優先して利用する．
- 手動給餌とスケジュールの追加・削除は，ハイドレーション後にページを再読み込みせず反映する．
- JavaScript または Wasm が利用できない場合も操作できるよう，通常の HTML フォームをフォールバックとして維持する．
- サーバ時刻，最終給餌時刻，クールタイム，スケジュールの変化は SSE 経由で自動反映する．

## 実行時設定

- `DATABASE_URL`: SQLite の接続先．既定値は `sqlite://pi-auto-feeder.sqlite3` とする．
- `FEEDER_MOCK`: `true` の場合は GPIO を操作せず給餌を模擬する．
- `CAMERA_MOCK`: `true` の場合はカメラの代わりにモック画像を配信する．

## セキュリティ

- Web サーバは Tailscale ネットワーク内からのみアクセス可能とする．
- インターネットへ直接公開する構成は採用しない．
- ユーザの追加・削除は Tailscale により管理する．

## 依存ライブラリ

- 新しいライブラリを追加する前に，標準ライブラリまたは既存ライブラリで実現できないか検討すること．
- 必要性が明確な場合のみ依存ライブラリを追加すること．

## ビルド・テスト

変更後は以下を実行し，成功することを確認する．

```sh
cargo fmt
leptosfmt src/**/*.rs
cargo clippy --all-targets -- -D warnings
cargo test
trunk build
```

## メンテナンスポリシー

- 会話中で繰り返し指示された内容は，ルール化できる場合は本書への反映を検討する．
- 重複する処理は共通化を検討する．
- 冗長な実装や文書は簡潔化を検討する．
- 可読性を優先し，過度な最適化は行わない．
- 文書は簡潔でありながら情報密度を高く保つ．

## AI エージェント向け

- 既存の設計・命名規則を尊重すること．
- 大規模なリファクタリングは提案に留め，利用者の同意なく実施しないこと．
- 不明な点は推測で実装せず，質問すること．
- 説明よりも実装を優先し，必要な場合のみ設計理由を付記すること．
- 新しい依存ライブラリの追加やアーキテクチャ変更は，必要性を説明した上で提案すること．
- 既存コードのスタイルに合わせ，必要以上にコードスタイルを変更しないこと．
